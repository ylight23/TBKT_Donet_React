import type {
  LocalDynamicField,
  LocalFieldSet,
  LocalFormConfig,
  LocalFormTabConfig,
} from 'apis/thamSoApi';

// Re-export type từ module gRPC gốc để tránh duplicate định nghĩa.
export type {
  LocalDynamicField,
  LocalFieldSet,
  LocalFormConfig,
  LocalFormTabConfig,
};
